// 全域設定檔
const CONFIG = {
    APP_NAME: "員工座談會提案平台",
    API_URL: "https://api.example.com/symposium",
    MAX_FILE_SIZE_MB: 10,
    ENVIRONMENT: "demo" // 'production' or 'demo'
};

console.log(`${CONFIG.APP_NAME} - 初始化完成`);
